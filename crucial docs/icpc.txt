#include<bits/stdc++.h>
using namespace std;

int main()
{
   freopen("sort.in", "r",stdin);
   freopen("sortout.txt", "w",stdout);
   int n,num;
   cin >> n;
   int arr[];
   for(int i =0 ; i < n ; i++)
    {
      cin >> num;
      arr[i]=num;
    }
    sort(arr, arr+n);
 
}